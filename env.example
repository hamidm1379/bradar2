# Telegram Client Configuration
# Copy this file to .env and fill in your actual values

# Get your API credentials from https://my.telegram.org/apps
API_ID=12345678
API_HASH=your_api_hash_here

# Session name (optional, defaults to "bot_session")
# This will create a file like "bot_session.session"
SESSION_NAME=bot_session

# Source channel (where the bot listens for posts)
# Use channel username with @ (e.g., @channel_name) or channel ID (e.g., -1001234567890)
# Example with username:
SOURCE_CHANNEL=@channel_name

# Target channel (where the bot sends modified posts)
# Use channel username with @ (e.g., @target_channel) or channel ID (e.g., -1001234567891)
# Example with username:
TARGET_CHANNEL=@target_channel
